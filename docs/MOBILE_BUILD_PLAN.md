# 48-hour mobile build plan

## Day 1
- Create GitHub repo from this starter.
- Deploy to a cloud host.
- Confirm `/` and `/erp` work.
- Create Solari key.
- Set `APP_BASE_URL` to the public preview.
- Call `/api/solari/verify`.
- Confirm Solari reads the three ERP facts.

## Day 2
- Add model provider.
- Build `decomposeObjective()`.
- Build `parseEmployeeUpdate()`.
- Persist objective tree and updates.
- Wire "Report blocker" to verification endpoint.
- Show verification state in UI.
- Propagate risk to parent expectations.
- Add manager support recommendation.

## Laptop day
- polish responsive UI
- add session replay link
- record 45–60 second demo
- improve README + architecture diagram
- optionally add Solari Desktop path for a GUI-only legacy system
